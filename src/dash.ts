/**
 * @affectively/aeon-foundation/dash
 *
 * Distributed state management with CRDT-based sync and WebRTC.
 *
 * @example
 * ```typescript
 * import { createStore } from '@affectively/aeon-foundation/dash';
 * ```
 */
export * from '@affectively/dash';
